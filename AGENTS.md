# Repository Rules

## Validation

- Before declaring work complete, always run the repository validation script when one exists.
- For lint validation in this repository, the required command is `./scripts/check.sh lint`.
- Direct tool commands such as `ruff check` or `ruff format --check` may be used while iterating, but they do not replace the final repository-level validation step.
